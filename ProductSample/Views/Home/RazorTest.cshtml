@model Int32[]

@{
    ViewBag.Title = "RazorTest";
}

<h2>RazorTest</h2>

@*@{ 
    @:Hello World
    <p>This is html code.</p>
}*@

@foreach (var i in Model)
{
    @i
}

@{ 
    string s = string.Join(" ", Model.Select(p => p));
    
    @s
}

<ul>
        @foreach (var i in @Model)
        {
            <li>@i</li>
        }
</ul>
